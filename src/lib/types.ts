// Get rid of this file if possible

export type User = {
  id: string
  isMember: boolean
  username: string
  avatarUrl: string
  email: string
  accessToken: string
  refreshToken: string
  discordId: string
}
